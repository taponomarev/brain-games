name: Linter

on: push

jobs:
  start-linter:
    runs-on: ubuntu-20.04
    steps:
      - name: Run command make lint
        run: make lint
        env:
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}